#ifndef OPJ_CONFIG_H
#define OPJ_CONFIG_H

/*
 * Minimal OpenJPEG config header for Android JNI wrapper builds that use
 * prebuilt shared libraries. The bundled openjpeg.h requires this include,
 * but our bridge only needs public API declarations.
 */

#endif /* OPJ_CONFIG_H */
